<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.salazar_geradordenpc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launchera"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launchera_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GerarActivity" />
        <activity android:name=".config" />
        <activity android:name=".manual" />
        <activity android:name=".favoritos" />
        <activity android:name=".npc_favorito"></activity>
    </application>

</manifest>